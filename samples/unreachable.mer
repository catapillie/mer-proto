func step(x: f64) -> f64
    = case
    {
        x < 0.0 then 0.0
        x >= 0.0 then 1.0
        otherwise unreachable
    }

var f = -1.0
while f <= 1.0 do
{
    debug step(f)
    f = f + 0.2
}

return
