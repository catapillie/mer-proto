func yes() -> bool {
    return 1 + 2 <= 3
}

func no() -> bool {
    return true == false
}

func log(b: bool) -> bool {
    debug b
    return b
}

if log(yes()) and log(no()) then debug 1 else debug 0
if log(yes()) or log(no()) then debug 1 else debug 0

return
